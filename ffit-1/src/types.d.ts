export type Section = {
    name: string;
    time_start: stirng;
    content: string;
}

export type Article = {
    id: string;
    user_id: string;
    content: string;
}

export type User = {
    id: string;
    name: string;
}
